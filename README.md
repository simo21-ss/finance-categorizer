# Title: Finance Categorizer (Rules-based)

## Stack: Node.js (Express), SQLite (Prisma), React (Vite), TypeScript

Run:

backend: npm i && npm run dev

frontend: npm i && npm run dev

## MVP:

import transactions

## manage rules

categorize + summaries